import React from 'react';

const Error = ({mensaje}) => {
   return(
     <div className="" style={{color:'red', marginBottom:'10px',textAlign:'center'}}>{mensaje}</div>
   )
     
}
 
export default Error;